#include <bits/stdc++.h>

using namespace std;

int n, p;
int energy[1005];

int dp() {
    for (int i = 0; i <; ++i) {

    }
};

int main() {
    scanf(" %d %d", &n, &p);
    for (int i = 0; i < n; ++i) {
        scanf(" %d", &energy[i]);
    }
    dp();
    return 0;
}