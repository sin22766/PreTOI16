#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, d, a;
    cin >> n >> d;
    vector<pair<int, int>> v;
    for (int i = 0; i < n; ++i) {
        cin >> a;
    }
    return 0;
}