#include <bits/stdc++.h>

using namespace std;

int main() {
    int T, n, S;
    scanf(" %d", &T);
    for (int t = 0; t < T; ++t) {
        scanf(" %d %d", &n, &S);
        int arr[n];
        for (int i = 0; i < n; ++i) {
            scanf(" %d", &arr[i]);
        }
    }
    return 0;
}

