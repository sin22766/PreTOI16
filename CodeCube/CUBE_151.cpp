#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m, k, t;
    int p, s;
    cin >> n >> m >> k >> t;
    vector<pair<int, int>> shoe;
    for (int i = 0; i < m; ++i) {

    }
    for (int i = 0; i < k; ++i) {
        cin >> p >> s;
        shoe.emplace_back(p, s);
    }
    return 0;
}