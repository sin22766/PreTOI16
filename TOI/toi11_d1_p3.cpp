#include <bits/stdc++.h>

using namespace std;

int main() {
    int m, n, k;
    cin >> m >> n >> k;
    int height[m][n];
    for (int i = 0; i < m; ++i) {
        for (int j = 0; j < n; ++j) {
            cin >> height[i][j];
        }
    }
    return 0;
}