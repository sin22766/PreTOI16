#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, q;
    scanf(" %d %d", &n, &q);
    int doll[n];
    for (int i = 0; i < n; ++i) {
        scanf(" %d", &doll[i]);
    }
    int l, r, k;
    for (int i = 0; i < q; ++i) {
        scanf(" %d %d %d", &l, &r, &k);

    }
    return 0;
}
