#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    scanf(" %d %d", &n, &m);
    int t, a, b;
    for (int i = 0; i < m; ++i) {
        scanf(" %d %d %d", &t, &a, &b);
        for (int j = 1; j <= n; ++j) {

        }
    }

    return 0;
}

